"""Models (placeholder).

Planned: ViT-S/16 context encoder, predictor, EMA target encoder, JEPA wrapper
(latent-MSE). See PROJECT_PLAN.md phase P4. No implementation yet — model code
waits for sign-off on the plan and the masking approach.
"""
