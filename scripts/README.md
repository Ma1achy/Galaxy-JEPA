# Scripts (placeholders)

Entry points land with their phases (PROJECT_PLAN.md). None implemented yet.

- `pull_data` — GZ2 cutouts via `galaxy-datasets` + CasJobs/SkyServer metadata join (P2)
- `run_pretrain` — config-driven JEPA pretraining (P5)
- `run_probe` — probing harness + controls + figures (P6–P7)
